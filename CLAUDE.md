# HR 系统项目说明与开发约束

## 一、项目目标

本项目是供 HR 使用的人员管理系统，目标是把人员主档案、任职历史、组织关系、录用入职、合同、人员履历和统计视图逐步建设为可追溯的业务系统。

- `frontend/`：React + TypeScript 前端。
- `backend/`：NestJS + Prisma 后端。
- `shared/`：前后端共用的 TypeScript 类型与接口契约。
- 完整业务数据以 MySQL 和 Prisma 模型为准。
- 业务页面按用户提供的字段顺序实现；没有可靠数据来源的字段必须保留位置并显示 `--`，不得用相似字段冒充。
- 本项目当前仅面向 HR 使用，暂不在项目约束中设置独立的字段级敏感权限规则；如后续需要，另行制定 plan，不得在实现页面时自行扩展权限含义。

## 二、人员主档案与重新入职

- `employees` 表示自然人的长期主档案，不表示某一次任职。
- 员工离职后重新入职时，沿用原员工主档案及已有历史资料，不创建重复人员。
- 每次入职、离职和重新入职应保存为独立的任职周期或业务记录，并关联同一个 `employee_id`。
- 不得因重新入职覆盖或删除原来的任职、合同、部门、履历、家庭、证件等历史资料。
- 员工重新入职时继续使用原工号，不生成新的工号；原工号不得因离职而分配给其他人员。

## 三、组织与任职关系

- 员工在同一时间不得同时属于多个部门；员工可以在不同时间通过独立任职记录保留多段部门历史。
- 部门关系必须通过独立任职关联记录表达，不得把单一 `organization_id` 作为完整的历史任职模型；同一时点只允许一条当前有效的部门任职关系。
- 每条任职关系至少应能表达员工、组织、开始时间、结束时间、有效状态，以及是否为主要任职；部门与职位是相互独立的字段和业务维度，互不依赖。
- 人员统计按当前唯一有效部门任职计入；历史统计按统计日期对应的有效任职关系计入，不因同一员工的不同历史记录重复计算。
- 现有 `employees.organization_id` 可在迁移期间兼容，但新增设计不得依赖它表达全部部门关系。

## 四、组织树

- 组织结构包含多个中心和部门，并应设计为可扩展的树，而不是固定两级。
- 组织节点使用自身 `id` 和 `parent_id` 关联上级，可通过类型区分中心、部门及未来新增的其他组织类型。
- 组织停用或归档后，历史任职仍必须能够识别原组织。
- 不得通过删除组织记录破坏员工任职历史。

## 五、历史记录

- 部门、岗位、职级、上级、人员类型、合同及任职状态等变化必须保留历史，不得直接覆盖后只留下当前值。
- 有生命周期的关联记录应使用生效时间、结束时间和状态表达当前与历史。
- 更新当前关系时，通常先结束原有效记录，再创建新记录。
- 详情、列表和分析接口应明确查询“当前有效记录”还是“完整历史记录”。

## 六、人员类型

第一版人员类型为：

- 内部员工
- 实习生
- 劳务人员

三类人员共用员工主档案，通过人员类型及任职周期区分，不得复制三套员工主表。人员类型发生变化时需要保留历史。

## 七、部门、岗位、职级与职务

- 部门和职位是相互独立的字段和业务维度，二者没有依赖关系；部门发生变化不得自动修改职位，职位发生变化也不得自动修改部门。
- 第一版将岗位、职级和职务分别建立数据表，并由员工任职记录分别关联；部门关系通过独立的任职关联记录维护。
- 该划分允许后续通过 Prisma Migration 调整，但不得在未迁移现有数据时随意合并或删除字段。

## 八、多上级汇报关系

- 一个员工在同一时点最多只有一个当前有效的主要上级；主要上级可以为空。除主要上级外，是否允许同时存在其他关系类型的上级，按具体汇报关系业务配置确定。
- 员工可以不存在任何当前有效的上级关系，但一条已经建立的汇报关系必须明确关联上级人员，不创建上级为空的占位关系。
- 上级可以来自其他部门，不要求与下属属于同一部门；允许建立指向下级人员的汇报关系，但不得将员工设置为自己的上级，也必须防止直接或间接形成循环汇报关系。
- 汇报关系使用独立关联记录，不得只在员工表保存一个固定的 `manager_id`。
- 每条汇报关系应能表达下属、上级、关系类型、是否主要关系、开始时间、结束时间和状态；同一有效范围内主要上级最多一个。
- 上级变化后保留旧关系历史。上级离职时，通过职责转交或其他业务流程将相关关系转给指定人员：结束原关系并建立指向指定人员的新关系，同时保留原关系和转交历史，不得静默覆盖原记录。
- 两端都通过员工 ID 关联，但汇报关系记录有自己独立的主键 ID。
- “汇报关系”和“汇报关系图”两个页签必须保留；具体字段和图形规则尚未确认时，保持占位并标明待确认。

## 九、审批规则

- 员工信息修改是否需要审批及其具体适用范围，由 HR 确定；不得在页面或代码中自行假定所有修改都必须审批或都无需审批。
- 审批支持多级流程；每个特定业务任务使用与其匹配的特定审批流程，不采用所有业务共用一条固定审批链。
- 审批流程由 HR 负责设置，并支持由 HR 导入、导出和修改流程定义。审批级数、审批节点、审批人及其他流程配置以已发布的流程定义为准，不由员工部门或单一上级字段自动推断。
- 审批人变更通过审批流程配置处理，HR 可以设置变更后的审批人或相应规则；不得静默替换审批历史中的原审批人。
- 审批通过后，才将申请内容写入正式业务数据；审批中的申请不能直接覆盖正式数据。
- 审批流程最终结果为“不通过”时，整个申请视为不通过。审批状态及草稿、提交、退回、驳回、撤回等具体枚举和流转规则仍待确认。
- HR 可以查看全部审批记录；部门负责人可以查看本部门及其所有下级组织范围内的审批记录；实际经过某审批流程的人员可以查看其参与过的对应审批记录。后端必须强制执行这些可见范围，前端隐藏不能替代权限校验。
- 审批历史永久保留，不得因流程完成、人员离职或组织调整而删除；具备最大权限的人员可以修改审批记录，但修改本身必须保留审计痕迹，不得无痕覆盖历史。
- 审批规则未明确的业务不得擅自新增审批节点、审批人、加签、会签或代理行为。

## 十、证件及人员附属资料

- 一个员工允许拥有多种证件，证件使用独立表并通过 `employee_id` 关联员工。
- 家庭成员、教育经历、工作履历、合同、证件、任职和汇报关系等一对多数据必须使用独立记录。
- 简历、员工详情、人事看板和报表可以组合多张表，不为组合展示重复保存整份数据。
- 证件、附件和其他人员资料只能使用真实业务系统已有数据或明显虚构的测试数据；不在代码、种子数据、测试、日志、README 或 Git 历史中写入真实人员资料。

## 十一、停用与归档

- 正式业务记录采用停用、撤销或归档，不进行物理删除。
- 有生命周期的业务表应按需要提供状态、归档时间、归档人和归档原因。
- 停用或归档记录不能再用于新业务选择，但必须可供历史详情、审计和报表查询。
- 只有明确属于未进入正式流程的错误或临时数据，且业务规则允许时，才可物理删除；执行前必须确认影响。

## 十二、部门数据范围

- 部门范围权限为“本部门及其所有下级组织”，不是只匹配一个部门 ID。
- 后端应根据组织树计算可访问组织集合，并在数据库查询或服务层强制应用。
- 人员当前详情和当前业务列表按员工唯一的当前有效部门任职判断范围；历史业务记录按业务发生日期对应的有效部门任职判断范围。
- 前端隐藏按钮只用于改善体验，不能替代后端权限校验。

## 十三、数据表与关联原则

- 按业务对象和生命周期拆表，不按页面机械地“一页一表”，也不得把所有字段堆入一张大表。
- 每张表有自己的主键 `id`；使用 `employee_id`、`organization_id` 等外键关联，不要求不同表的主键相同。
- 可计算的统计值优先查询或聚合得到，避免与来源数据重复存储而产生不一致。

## 十四、两种运行模式

- `DEMO_MODE=true`：免数据库精简演示仅保证现有核心员工功能，完整 HR 模块以 MySQL 模式为准。
- `DEMO_MODE=false`：使用 `backend/.env` 中的 MySQL 配置，完整业务字段、历史关系和新增模块以 Prisma/MySQL 为准。
- 同一个已经对外提供的 API 不能因数据来源不同而偷偷返回含义冲突的字段；只支持 MySQL 的新模块必须明确要求数据库模式。
- 演示模式只能保存虚构内存数据，后端重启后恢复初始状态。

## 十五、数据库变更纪律

- 使用 Prisma schema 和 Prisma Migration 管理 MySQL 表结构，不直接无记录地手工修改正式数据库。
- 新增字段后已有数据无法立即补齐时，先设为可选或提供安全默认值，再回填数据，最后视业务需要改为必填。
- 重命名、删除字段、修改字段类型、拆表或更改外键属于可能丢失数据的操作；执行前必须检查生成的 SQL、制定数据迁移方案并备份。
- 每次迁移都应保持历史数据、外键完整性和现有 API 的兼容性，除非用户明确批准破坏性变更。

## 十六、字段实现与邮箱约定

- 用户提供的表格字段必须保持原有顺序。
- 有明确 Prisma 字段、关系或已确认业务来源的字段，才能接入真实数据。
- 没有来源、来源不可靠或业务含义尚未确认的字段，后端返回 `null`，前端显示 `--`；不得使用编码、创建时间或其他近似字段代替。
- 列表字段名为“邮箱”或“电子邮箱”时，默认读取 `Employee.workEmail`（数据库 `employees.work_email`）。
- “个人邮箱”读取 `Employee.personalEmail`；“直线经理邮箱”读取主要经理的 `Employee.workEmail`；Offer 候选人个人邮箱及明确命名为 `personalEmail` 的字段按其自身模型处理。
- 内部公司统一使用 `EmployingCompany`（“全日制公司”）目录；仅合同协议通过 `EmployeeAgreement.employingCompanyId` 关联，员工任职不再保存公司关联。禁止新增或使用独立的合同签订公司/`signingOrganizationId` 映射；合同记录保留其自身的全日制公司关系，以便员工后续变更公司时继续识别历史合同归属。
- 页面宽表是多张业务表的组合视图，不得据此把数据模型合并成一张大表。

## 十七、开发工作方式

- 新对话开始时先读取本文件、[`docs/PROJECT_STATUS.md`](docs/PROJECT_STATUS.md)、相关页面/API/Prisma 模型和 `git status`。
- 只针对用户指定页面及其直接依赖做增量查询、实现和聚焦测试，不重复全仓扫描或无关重构。
- 未经用户明确要求，不连接或修改真实业务数据库，不运行 migration、seed 或应用启动，不执行 commit 或 push。
- 发现当前代码与字段结构不一致时，先以代码和 Prisma schema核对真实来源；不为填满页面而新增未经确认的业务规则。
- 部门任职唯一性、主要上级和审批流程的已确认规则以本文件为准；审批状态流转、其他关系类型上级以及汇报关系页面/关系图仍未确认，影响业务行为时集中向用户确认。
- 前端基础表格页面的整体格式、布局、间距、交互样式和字体大小，统一以“人员”页面为标准；新增或调整列表页时优先复用人员页面的表格、筛选栏、分页、空状态和操作列样式，除非用户明确指定例外。

## 十八、当前工作接续入口

当前菜单、接口、页面字段、已完成工作、测试结果和已知限制统一记录在 [`docs/PROJECT_STATUS.md`](docs/PROJECT_STATUS.md)。该文件是新对话的项目进展快照；数据库模型仍以 [`backend/prisma/schema.prisma`](backend/prisma/schema.prisma) 为准，接口细节仍以代码和 [`docs/API.md`](docs/API.md) 为准。
